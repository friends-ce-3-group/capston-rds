name: Terraform CI and Manual Destroy

on:
  push:
    branches:
      - "*" # Trigger CI on pushes to the 'working' branch

  workflow_dispatch: {} # Allow manual triggering for destroy

env:
  AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
  AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}

  
jobs:
  ci:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: ./testing

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v2

      - name: Init
        run: terraform init

      - name: Validate
        run: terraform validate

      - name: Plan
        run: terraform plan

  # destroy:
  #   runs-on: ubuntu-latest
  #   needs: ci # Ensure CI job completes before running destroy

  #   steps:
  #     - name: Checkout Repository
  #       uses: actions/checkout@v4

  #     - name: Setup Terraform
  #       uses: hashicorp/setup-terraform@v2

  #     - name: Init
  #       run: terraform init

  #     - name: Plan Destroy
  #       run: terraform plan -destroy -out=destroy.tfplan
  #       env:
  #         TF_VAR_destroy: 1

  #     - name: Apply Destroy
  #       if: github.event.inputs.confirmDestroy == 'true'
  #       run: terraform apply destroy.tfplan
